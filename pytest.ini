[pytest]
pythonpath = .
asyncio_mode = auto
addopts = -v --cov=internal --cov=cmd --cov-report=term-missing
markers =
    unit: Unit tests
    integration: End-to-end integration tests
    stress: High concurrency stress tests
    protocol: Tests for malformed TCP requests and protocol validation
